# The board holds a WebSocket open at /ws for hours to stream live.json. Without this map
# the Connection header arrives as "close" and the handshake fails.
map $http_upgrade $connection_upgrade {
    default upgrade;
    ''      close;
}

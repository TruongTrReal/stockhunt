"""Three independent implementations of the same backtest.

`vector` sweeps, `reference` arbitrates, `nautilus` validates. `parity.py` at the
project root asserts they agree — disagreement fails the build.
"""
